# App Icon

Place `appicon.svg` (background) and `appiconfg.svg` (foreground) here.
The `.csproj` references them via:

```xml
<MauiIcon Include="Resources/AppIcon/appicon.svg" ForegroundFile="Resources/AppIcon/appiconfg.svg" Color="#512BD4"/>
```

Replace with actual SVG files before publishing.
