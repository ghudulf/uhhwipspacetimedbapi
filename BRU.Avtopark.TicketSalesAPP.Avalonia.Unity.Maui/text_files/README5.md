# Splash Screen

Place `splash.svg` in Resources/Splash/.
The `.csproj` references it via:

```xml
<MauiSplashScreen Include="Resources/Splash/splash.svg" Color="#512BD4" BaseSize="128,128"/>
```

Replace with actual SVG before publishing.